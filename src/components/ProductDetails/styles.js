import styled from 'styled-components';
import {ProductProperty, ProductName} from '../Product/styles';

export const Name = styled(ProductName)`
  margin-vertical: 12px;
`;

export const Property = styled(ProductProperty)`
  font-weight: 600;
`;

export const Text = styled(ProductProperty)`
  margin-vertical: 6px;
`;
